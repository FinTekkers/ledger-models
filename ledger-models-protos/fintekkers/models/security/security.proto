syntax = "proto3";

package fintekkers.models.security;

import "fintekkers/models/util/decimal_value.proto";
import "fintekkers/models/util/local_date.proto";
import "fintekkers/models/util/local_timestamp.proto";
import "fintekkers/models/util/uuid.proto";

import "fintekkers/models/security/identifier/identifier.proto";
import "fintekkers/models/security/bond/issuance.proto";
import "fintekkers/models/security/product_type.proto";
import "fintekkers/models/security/instrument_type.proto";
import "fintekkers/models/security/security_quantity_type.proto";
import "fintekkers/models/security/coupon_frequency.proto";
import "fintekkers/models/security/coupon_type.proto";
import "fintekkers/models/security/index/index_type.proto";

option java_multiple_files = true;

option java_outer_classname = "SecurityProtos";

message SecurityProto {
  string object_class = 1;
  string version = 2;

  //Primary Key
  fintekkers.models.util.UUIDProto uuid = 5;
  fintekkers.models.util.LocalTimestampProto as_of = 6;

  // When true, this message is a lightweight reference — only uuid is populated.
  // The caller must resolve the full entity by calling SecurityService.GetByIds
  // with this UUID. Used when embedding a SecurityProto inside another message
  // (e.g. PriceProto.security, TransactionProto.security) to avoid duplicating
  // the full security data. See docs/adr/is_link_pattern.md for details.
  bool is_link = 7;

  util.LocalTimestampProto valid_from = 8;
  util.LocalTimestampProto valid_to = 9;

  ProductTypeProto product_type = 10;

  // Orthogonal to product_type — see instrument_type.proto.
  // CASH (positionable), DERIVATIVE (value derives from underlying),
  // REFERENCE_INDEX (observational only).
  InstrumentTypeProto instrument_type = 16;

  // Multi-leg strategy package legs — each leg is itself a Security
  // referenced by UUID using the is_link=true pattern (see
  // docs/adr/is_link_pattern.md). See hierarchy-examples.md for the
  // pattern: butterflies, calendar spreads, iron condors, etc. are not
  // productTypes; they're a Security whose product_type is the
  // underlying vanilla type with `legs` populated.
  //
  // Each leg MUST have is_link=true with uuid and as_of populated.
  // Resolve full legs via SecurityService.GetByIds.
  //
  // WIRE COMPATIBLE with the prior SecurityIdProto type at this tag:
  // SecurityIdProto carried uuid at tag 1, identical to SecurityProto.
  // Legacy persisted bytes parse correctly under the new type.
  repeated SecurityProto legs = 17;

  // Soft-delete marker. null/unset = active record; non-null = soft-deleted
  // at this timestamp. SecurityService.Search and GetByIds filter out
  // soft-deleted records by default. Setting deleted_at via CreateOrUpdate
  // is a soft-delete; clearing it on a subsequent CreateOrUpdate resurrects
  // the record.
  // See: https://github.com/FinTekkers/second-brain/issues/188
  fintekkers.models.util.LocalTimestampProto deleted_at = 15;

  //Biz fields
  string asset_class = 11;
  string issuer_name = 12;
  SecurityProto settlement_currency = 13;
  SecurityQuantityTypeProto quantity_type = 14;

  string description = 41;

  // All known identifiers for this security. The primary identifier (used as
  // the human-readable ID) is the first entry. Secondary identifiers follow.
  // For Gilts, entry 0 will be {type=ISIN, value="GB..."}; for US Treasuries,
  // entry 0 will be {type=CUSIP, value="912828..."}.
  repeated security.IdentifierProto identifiers = 42;

  // ============================================================================
  // v0.4.0 / #278: the legacy "flat" product fields (tags 40, 50, 60-67,
  // 70-72, 80, 90-92) were removed in v0.4.0 and are reserved here so they
  // cannot be re-introduced. All product-specific data now lives in the
  // structured shape below (bond_details / tips_extension / frn_extension /
  // non_bond_details oneof). See second-brain#277 for the cutover rationale
  // and second-brain#272 (v0.3.0 P1) for the additive structured shape that
  // preceded this deletion.
  // ============================================================================
  reserved 40, 50, 60, 61, 62, 63, 64, 65, 66, 67, 70, 71, 72, 80, 90, 91, 92;
  reserved "identifier", "cash_id", "coupon_rate", "coupon_type", "coupon_frequency",
           "dated_date", "face_value", "issue_date", "maturity_date", "issuance_info",
           "base_cpi", "index_date", "inflation_index_type", "index_type",
           "spread", "reference_rate_index", "reset_frequency";

  // ============================================================================
  // Structured product details — v0.4.0 shape (#265 / #272 / #277 / #278).
  //
  // Bond-shape products use the canonical `bond_details` field for the 8
  // shared fields (coupon_rate, coupon_type, coupon_frequency, dated_date,
  // face_value, issue_date, maturity_date, issuance_info). TIPS and FRN
  // additionally populate `tips_extension` / `frn_extension` with their
  // subtype-specific extras — these CO-EXIST with bond_details and do NOT
  // replace it.
  //
  // Non-bond products (Cash, Equity, Index, FxSpot) use the
  // `non_bond_details` oneof since they have no shared shape and are
  // mutually exclusive.
  // ============================================================================

  // Bond-shape details. Populated for any product_type descending from BOND
  // in hierarchy.json (TBILL, TREASURY_NOTE, TREASURY_BOND, TIPS,
  // TREASURY_FRN, STRIPS, SOVEREIGN_BOND, CORP_BOND, MUNI_BOND, ...).
  // Single canonical home for the 8 shared bond fields. Null for non-bonds.
  BondDetailsProto bond_details = 200;

  // TIPS-specific extras. Populated when product_type == TIPS.
  // Co-exists with bond_details (does NOT replace it).
  TipsExtensionProto tips_extension = 201;

  // FRN-specific extras. Populated when product_type == TREASURY_FRN.
  // Co-exists with bond_details (does NOT replace it).
  FrnExtensionProto frn_extension = 202;

  // Non-bond product details. These ARE mutually exclusive — Cash, Equity,
  // Index, FxSpot have no shared shape — so a oneof fits.
  oneof non_bond_details {
    IndexDetailsProto index_details = 203;
    EquityDetailsProto equity_details = 204;
    CashDetailsProto cash_details = 205;
    FxSpotDetailsProto fx_spot_details = 206;
  }
}

// Bond security details: fixed-rate coupon bonds (US Treasuries, corporates, etc.)
message BondDetailsProto {
  fintekkers.models.util.DecimalValueProto coupon_rate = 1; // Expressed as a decimal fraction (0.05=5%, 0.0075=0.75%). Do NOT use percentage form (5.0 will be rejected).
  CouponTypeProto coupon_type = 2;
  CouponFrequencyProto coupon_frequency = 3;
  fintekkers.models.util.LocalDateProto dated_date = 4;
  fintekkers.models.util.DecimalValueProto face_value = 5;
  fintekkers.models.util.LocalDateProto issue_date = 6;
  fintekkers.models.util.LocalDateProto maturity_date = 7;
  repeated fintekkers.models.security.bond.IssuanceProto issuance_info = 8;
}

// TIPS (Treasury Inflation-Protected Securities) extras.
// Carries ONLY the TIPS-specific fields; shared bond fields live in
// SecurityProto.bond_details. Populated when product_type == TIPS.
message TipsExtensionProto {
  fintekkers.models.util.DecimalValueProto base_cpi = 1; //Reference CPI at bond issuance (e.g. 256.394)
  fintekkers.models.util.LocalDateProto index_date = 2;  //The date the base CPI was observed
  fintekkers.models.security.index.IndexTypeProto inflation_index_type = 3; //Which inflation index (e.g. CPI_U)
}

// Floating Rate Note (FRN) extras.
// Carries ONLY the FRN-specific fields; shared bond fields live in
// SecurityProto.bond_details. Populated when product_type == TREASURY_FRN.
message FrnExtensionProto {
  fintekkers.models.util.DecimalValueProto spread = 1;                       //Fixed spread over the reference rate, in basis points
  fintekkers.models.security.index.IndexTypeProto reference_rate_index = 2;  //Which floating rate benchmark (e.g. SOFR)
  CouponFrequencyProto reset_frequency = 3;                                  //How often the floating coupon rate resets
}

// Index security details (e.g. CPI-U, SOFR index, S&P 500).
message IndexDetailsProto {
  fintekkers.models.security.index.IndexTypeProto index_type = 1;

  // Populated when QuerySecurityRequestProto.lookthrough=true. Server-side
  // resolver computes constituents for the request asOf. Each constituent
  // is a SecurityProto with is_link=true (uuid + as_of populated; resolve
  // full security via SecurityService.GetByIds).
  // See docs/adr/is_link_pattern.md.
  repeated SecurityProto constituents = 2;
}

// Equity security details. Minimal for now — placeholder for future fields
// (e.g. shares_outstanding, dividend_yield, sector).
message EquityDetailsProto {
  // No equity-specific fields exist yet. This message serves as a type marker
  // in the oneof and a home for future equity-specific fields.
}

// Cash security details.
message CashDetailsProto {
  string cash_id = 1; //e.g. "USD", "EUR", "GBP"
}

// FX spot pair details.
// Represents a spot foreign exchange rate between two currencies.
// The pair is expressed as: price = units of quote_currency per 1 unit of base_currency.
// Example: USD/GBP with convention UNITS_OF_QUOTE_PER_BASE means the price is
// "how many GBP you receive for 1 USD" (e.g. 0.79).
message FxSpotDetailsProto {
  SecurityProto base_currency = 1;  //The currency being bought/sold (e.g. USD cash security). Must be a CashDetailsProto with is_link=true.
  SecurityProto quote_currency = 2; //The currency in which the price is expressed (e.g. GBP cash security). Must be a CashDetailsProto with is_link=true.
  string convention = 3;            //Quoting convention — always "UNITS_OF_QUOTE_PER_BASE" for spot FX (ISO standard)
}
