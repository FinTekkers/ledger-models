#Rust related folders
ledger-models-rust/target
ledger-models-rust/protos
valuation-service/target

#Java / Gradle related folders (Java build)
.gradle
ledger-models-java/bin

#Python related folders
.pytest_cache
__pycache__
*egg-info
.venv/
venv/

#Build related folders:
build/
dist/
out/
target
target/**

#IDE related folders
.vscode
.idea

#Mac related folders
.DS_Store

#Local tooling state (Claude Code, etc.)
.claude/


#npm
node_modules/
.npmrc

#Secrets / credentials
.env
*.pem
*.key
credentials.json

# hierarchy.json language-package mirrors — generated at build time by
# sync-hierarchy-mirrors.sh from the canonical at
# ledger-models-protos/hierarchy.json. ONLY the canonical is committed;
# the three mirrors are build artifacts so cargo / npm / pip can bundle
# them into their tarballs. See registry-versioning.md.
ledger-models-rust/hierarchy.json
ledger-models-javascript/hierarchy.json
ledger-models-python/fintekkers/wrappers/models/security/hierarchy.json
